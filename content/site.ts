/**
 * 사이트 전역 메타/회사 정보.
 *
 * ⚠️ **미확정 값은 플레이스홀더가 아니라 `null` 이다.** (2026-09-14 변경)
 *    이전에는 `000-00-00000` / `02-0000-0000` / `테헤란로 000` 같은 가짜 값을
 *    넣어 뒀는데, 커스텀 도메인이 붙은 뒤로 그게 **실제 운영 사이트에 그대로
 *    노출됐다.** 없는 번호를 `tel:` 링크로 걸어 두는 건 그 자체가 결함이고,
 *    사업자등록번호·주소는 사업자 표시 정보라 틀린 값이 더 나쁘다.
 *    → `null` 이면 각 화면이 **그 줄을 통째로 접는다.** 값이 확정되면 여기
 *      한 줄만 채우면 footer·about·contact·CTA 가 한꺼번에 복구된다.
 *
 *    확정: 상호 WITUS(위투스), 정식 법인명, 대표자명, 대표 이메일,
 *          개인정보 보호책임자.
 *    미확정(`null`): 사업자등록번호(등록 전), 주소(확인 전), 전화, 팩스.
 *    → 남은 항목은 `doc/05-content-guide.md` 체크리스트로 관리한다.
 *
 * ⚠️ 전자금융 관련 광고성 표현은 교체 시 준법감시(컴플라이언스) 검토 필수.
 */

export const site = {
  /**
   * 브랜드 표기. 화면 전반과 `<title>` 템플릿에 쓰인다.
   *
   * `Work In Trust` + `us` 에서 온 이름이다 — "우리" 가 들어가 팀의 뉘앙스를 담는다.
   */
  name: 'WITUS',
  /** 한글 표기. 헤더에서 브랜드 옆에 함께 노출한다. */
  nameKo: '위투스',
  /**
   * 로고 락업의 영문 보조 문구. 푸터 브랜드 블록에 노출한다.
   *
   * ⚠️ **머리글자를 1:1로 맞추려 하지 말 것.** 이전 표기 `WORK IN TRUST US` 는
   *    `W-I-T-U-S` 다섯 글자를 그대로 맞추려다 **영어 비문**이 됐다
   *    (`Work in trust us` 는 문장으로 성립하지 않는다).
   *    `with` 를 넣어 문법을 먼저 맞추고, 약어는 `WIT` + `US` 로 자연스럽게
   *    드러나게 했다 — 태그라인의 머리글자를 세는 독자는 없다 (ADR-027).
   *
   * `with` 를 빼서 다시 `WORK IN TRUST US` 로 되돌리지 않는다. 그게 원래 문제였다.
   *
   * 헤더에는 넣지 않는다 — 헤더 락업은 `name` + `nameKo` 두 줄까지가 한계다.
   */
  motto: 'WORK IN TRUST WITH US',
  /**
   * 정식 법인명. 사업자 표시 정보(푸터·회사소개·개인정보처리방침)에만 쓴다.
   *
   * ## ⚠️ 지금 `주식회사` 를 붙이지 않는 이유 — 설립등기 전이다
   *
   * 법인 형태는 **주식회사로 확정**됐고(2026-09-14, 사용자) 한때
   * `주식회사 위투스` 로 넣었다. 그런데 **설립등기가 아직 안 됐다**(2026-09-14 확인).
   *
   * **상법 제20조는 회사가 아니면 상호에 회사임을 표시하는 문자를 쓰지 못하게
   * 한다**(위반 시 과태료, 제28조). "주식회사" 는 단순한 이름의 일부가 아니라
   * **법인격이 있다는 표시**다. 등기 전에 내걸면 없는 법인격을 주장하는 게 된다.
   *
   * 사용자 판단으로 **등기 완료까지 `WITUS`** 로 두기로 했다(2026-09-14).
   *
   * ## 등기가 끝나면
   *
   * 등기부등본 표기 **그대로** 넣는다. 전치/후치(`주식회사 위투스` vs
   * `위투스 주식회사`)까지 등기부를 따른다 — 사업자 표시 정보라 임의로 바꾸면
   * 틀린 게 된다. 국내 실무에서는 전치형이 더 흔하지만 **추측하지 말고 대조할 것.**
   *
   * ⚠️ **본문 산문에는 쓰지 않는다.** 산문에서는 브랜드 표기 `site.name` 을 쓴다.
   *    `주식회사 위투스 는 ...` 처럼 읽히면 어색하고, 법인명은 법적 표시가 필요한
   *    자리에만 있어야 의미가 산다.
   */
  legalName: 'WITUS', // TODO: 설립등기 후 등기부등본 표기로 교체
  tagline: '결제 인프라의 처음부터 끝까지',
  description:
    '밴 단말기·키오스크 공급, PG 영업대행, 전자금융업 등록, 금융 클라우드 이용등록, 금융결제원 오픈뱅킹 컨설팅까지. 결제 사업에 필요한 모든 절차를 한 팀이 끝냅니다.',
  // 절대 URL 은 여기 두지 않는다 — 이 모듈은 클라이언트 컴포넌트도 import 한다.
  //   → lib/site-url.ts (server-only) 의 `siteUrl` 을 쓴다.
  locale: 'ko_KR',
} as const

/**
 * 대표 이메일. 확정값(2026-09-13).
 *
 * ⚠️ **여기 한 곳만 바꾼다.** 대표 이메일과 개인정보 보호책임자 연락처가 **같은
 *    주소**이고, 두 군데에 각각 적어 두면 한쪽만 바꿨을 때 갈린다. 보호책임자
 *    연락처는 이 상수에서 파생시킨다. 처리방침에 나가는 값이라 갈리면 곤란하다.
 *
 * ⚠️ **도메인 메일(`@witus.kr`)로 옮기는 것을 권한다.** `witus.kr` 은 이미 확보돼
 *    있다. 전자금융업 등록·금융기관 제출 서류와 PG 영업대행 상대 B2B 에서
 *    `@gmail.com` 대표메일은 신뢰도에 불리하다.
 *    수신함을 바꾸지 않고도 가능하다 — `contact@witus.kr` 를 이 주소로 **전달**하고
 *    Gmail 의 "다른 주소에서 메일 보내기" 로 발신만 도메인 주소로 맞춘다.
 *    (가비아 메일 포워딩 또는 Cloudflare Email Routing, 둘 다 비용 없음)
 *    → 사용자는 **Google Workspace 도입 시 교체**할 예정이다(2026-09-13).
 */
const EMAIL = 'wituskr@gmail.com'

/** 대표자 성명. 개인정보 보호책임자를 겸한다. */
const CEO = '최봉균'

/**
 * 아직 확정되지 않은 표시 정보.
 *
 * `null` 은 "값이 없다" 가 아니라 **"아직 없으니 화면에서 그 줄을 접어라"** 는
 * 지시다. 소비하는 쪽(`site-footer`, `cta-band`, `contact`, `about`)은 모두
 * null 분기를 갖고 있다. 임의로 빈 문자열(`''`)로 바꾸지 말 것 — 빈 줄이 남는다.
 */
type Pending = string | null

export const company: {
  ceo: string
  bizNo: Pending
  address: Pending
  tel: Pending
  fax: Pending
  email: string
  privacyOfficer: string
  hours: string
} = {
  ceo: CEO,
  /** 사업자등록번호. **아직 등록 전**(2026-09-14, 사용자). 등록증 발급 후 채운다. */
  bizNo: null,
  /** 사업장 주소. **아직 확인 전**(2026-09-14, 사용자). 등록증 상 소재지를 넣는다. */
  address: null,
  /**
   * 대표 전화. 미개통.
   *
   * ⚠️ 여기에 값이 들어오면 `/contact` 히어로 문안이 **전화 우선**으로 바뀌고
   *    푸터·CTA 에 전화 버튼이 생긴다. 개통 전까지는 이메일 단일 창구다.
   * ⚠️ 개인정보처리방침 7항(보호책임자)에 전화 줄을 추가할지도 함께 판단한다.
   */
  tel: null,
  /** 팩스. 미개통. 현재 어느 화면에서도 쓰지 않는다. */
  fax: null,
  email: EMAIL,
  /**
   * 개인정보 보호책임자. `성명 / 연락처` 형식으로 푸터와 처리방침 5·7항에 노출된다.
   *
   * **대표자가 겸한다**(2026-09-13, 사용자 결정). 소규모 사업자에서 일반적인 형태다.
   * 연락처는 `EMAIL` 에서 파생된다 — 대표 이메일을 바꾸면 여기도 함께 바뀐다.
   *
   * ⚠️ 개인정보보호법상 보호책임자의 **연락처는 전화번호·전자우편 등 중 하나**면
   *    충족된다. 전화번호를 함께 적으려면 `company.tel` 이 실제 번호여야 한다
   *    (현재 `null`).
   */
  privacyOfficer: `${CEO} / ${EMAIL}`,
  hours: '평일 09:00 – 18:00 (점심 12:00 – 13:00)',
}

/**
 * `tel:` 링크 주소. 전화가 미개통이면 `null` 이다.
 *
 * 하이픈 제거 로직을 화면마다 반복하지 않기 위해 여기서 한 번만 파생시킨다.
 * **`null` 검사 없이 `href` 에 넣지 말 것** — 타입이 막아 준다.
 */
export const telHref: string | null = company.tel
  ? `tel:${company.tel.replace(/-/g, '')}`
  : null

/** `mailto:` 링크 주소. 대표 이메일은 항상 있으므로 `null` 이 아니다. */
export const mailHref = `mailto:${company.email}`

export const nav = [
  { href: '/services', label: '서비스' },
  { href: '/about', label: '회사소개' },
  { href: '/contact', label: '문의' },
] as const

/**
 * 홈 상단 신뢰 지표.
 *
 * ⚠️ 값이 `XXX` / `OO` 인 것은 **근거 자료가 없어 플레이스홀더로 치환한 것**이다.
 *    구체적인 숫자를 근거 없이 게재하면 표시광고법 리스크가 된다(허위·과장 광고).
 *    근거(계약 건수 대장, 설치 대장, 실적 집계 등)를 확보한 항목만 실제 숫자로
 *    바꾸고, 확보하지 못한 항목은 **숫자를 만들지 말고 이 배열에서 제거**한다.
 */
export const metrics = [
  { value: 'XXX+', label: '누적 진행 프로젝트' },
  { value: 'X,XXX대', label: '단말기 설치·운영' },
  { value: 'XX일', label: '평균 등록 소요' },
] as const

/**
 * 제휴사 표기. `components/logo-strip.tsx` 가 렌더한다.
 *
 * ⚠️ 값이 `VAN A` / `PG C` 처럼 **플레이스홀더다.** 제휴가 확정되지 않았다
 *    (2026-09-14, 사용자). 그래서 `features.partnerLogos` 로 섹션을 내려 두었다.
 *
 * ⚠️ **실명을 넣기 전에 서면 사용 동의를 받을 것.** 제휴사 상호·로고를 동의 없이
 *    게재하면 상표권·부정경쟁 문제가 된다. 특히 "연동합니다" 는 제휴 관계를
 *    주장하는 문장이라, 연동 실적이 없는 곳을 올리면 표시광고법 리스크가 된다.
 */
export const partnerLogos = [
  'VAN A',
  'VAN B',
  'PG C',
  'PG D',
  'BANK E',
  'CSP F',
] as const
